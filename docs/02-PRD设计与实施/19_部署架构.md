# PRD-19: 部署架构 (Docker Compose)

---
Status: active
Owner: 运维与平台工程
Last verified: 2026-09-11
Evidence: V2 私有服务器部署目标；空环境重建、镜像检查、健康检查、备份恢复和 SSE 验证为上线证据。
Supersedes: 旧版生产 Compose 声明
---

> 本文是目标部署架构。现有 Compose 仍需修复服务名、密钥、Nginx、镜像上下文和数据库初始化问题。

## 1. 架构总览

### 1.1 服务组成

```text
┌─────────────────────────────────────────┐
│             Nginx (Gateway)             │
│  - TLS 终止                             │
│  - 反向代理                             │
│  - 静态文件服务                         │
└──────────────┬──────────────────────────┘
               │
       ┌───────┴───────┐
       │               │
   ┌───▼────┐    ┌────▼────┐
   │  Web   │    │   API   │
   │ (React)│    │(FastAPI)│
   └────────┘    └────┬────┘
                      │
          ┌───────────┼───────────┐
          │           │           │
      ┌───▼───┐   ┌──▼───┐   ┌──▼─────┐
      │ Postgres│ │ Redis │  │ Worker │
      │(+pgvector)│└──────┘  │ (Celery)│
      └─────────┘             └────────┘
```

---

## 2. Docker Compose 配置

### 2.1 docker-compose.yml

```yaml
version: '3.8'

services:
  # 前端
  web:
    build:
      context: ./apps/web
      dockerfile: Dockerfile
    container_name: student-agent-web
    environment:
      - REACT_APP_API_URL=https://api.example.com
    networks:
      - app-network
    restart: unless-stopped

  # 后端 API
  api:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: student-agent-api
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:${POSTGRES_PASSWORD}@postgres:5432/student_agent
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - app-network
    restart: unless-stopped
    volumes:
      - ./apps/api/logs:/app/logs

  # 异步 Worker
  worker:
    build:
      context: ./apps/api
      dockerfile: Dockerfile
    container_name: student-agent-worker
    command: celery -A app.worker worker --loglevel=info
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql+asyncpg://postgres:${POSTGRES_PASSWORD}@postgres:5432/student_agent
      - REDIS_URL=redis://redis:6379/0
    depends_on:
      - postgres
      - redis
    networks:
      - app-network
    restart: unless-stopped

  # PostgreSQL
  postgres:
    image: pgvector/pgvector:pg15
    container_name: student-agent-postgres
    environment:
      - POSTGRES_DB=student_agent
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./infra/postgres/init.sql:/docker-entrypoint-initdb.d/init.sql
    ports:
      - "5432:5432"
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Redis
  redis:
    image: redis:7-alpine
    container_name: student-agent-redis
    command: redis-server --appendonly yes
    volumes:
      - redis-data:/data
    ports:
      - "6379:6379"
    networks:
      - app-network
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Nginx
  nginx:
    image: nginx:alpine
    container_name: student-agent-nginx
    volumes:
      - ./infra/nginx/nginx.conf:/etc/nginx/nginx.conf
      - ./infra/nginx/certs:/etc/nginx/certs
    ports:
      - "80:80"
      - "443:443"
    depends_on:
      - web
      - api
    networks:
      - app-network
    restart: unless-stopped

volumes:
  postgres-data:
  redis-data:

networks:
  app-network:
    driver: bridge
```

---

## 3. Nginx 配置

### 3.1 nginx.conf

```nginx
user nginx;
worker_processes auto;
error_log /var/log/nginx/error.log warn;
pid /var/run/nginx.pid;

events {
    worker_connections 1024;
}

http {
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for"';
    
    access_log /var/log/nginx/access.log main;
    
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    types_hash_max_size 2048;
    
    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript 
               application/json application/javascript application/xml+rss;
    
    # 上游服务
    upstream api_backend {
        server api:8000;
    }
    
    upstream web_backend {
        server web:3000;
    }
    
    # HTTP 重定向到 HTTPS
    server {
        listen 80;
        server_name example.com;
        return 301 https://$server_name$request_uri;
    }
    
    # HTTPS 服务
    server {
        listen 443 ssl http2;
        server_name example.com;
        
        # TLS 证书
        ssl_certificate /etc/nginx/certs/fullchain.pem;
        ssl_certificate_key /etc/nginx/certs/privkey.pem;
        
        # TLS 配置
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers HIGH:!aNULL:!MD5;
        ssl_prefer_server_ciphers on;
        ssl_session_cache shared:SSL:10m;
        ssl_session_timeout 10m;
        
        # 安全头
        add_header Strict-Transport-Security "max-age=31536000" always;
        add_header X-Frame-Options "DENY" always;
        add_header X-Content-Type-Options "nosniff" always;
        add_header X-XSS-Protection "1; mode=block" always;
        
        # 前端（SPA）
        location / {
            proxy_pass http://web_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # API
        location /api/ {
            proxy_pass http://api_backend;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # 超时设置
            proxy_connect_timeout 10s;
            proxy_send_timeout 30s;
            proxy_read_timeout 30s;
        }
        
        # SSE 流式接口（特殊配置）
        location /api/v1/chat/sessions {
            proxy_pass http://api_backend;
            
            # SSE 关键配置
            proxy_buffering off;
            proxy_cache off;
            proxy_read_timeout 300s;
            
            # HTTP/1.1 支持
            proxy_http_version 1.1;
            proxy_set_header Connection "";
            
            # 转发头
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # API 文档
        location /docs {
            proxy_pass http://api_backend;
            proxy_set_header Host $host;
        }
        
        # 健康检查
        location /health {
            proxy_pass http://api_backend;
            access_log off;
        }
    }
}
```

---

## 4. 环境变量

### 4.1 .env.example

```bash
# 应用
APP_NAME=StudentAgent
APP_ENV=production
DEBUG=false

# 数据库
POSTGRES_PASSWORD=your_secure_password_here
DATABASE_URL=postgresql+asyncpg://postgres:password@postgres:5432/student_agent

# Redis
REDIS_URL=redis://redis:6379/0

# 认证
JWT_SECRET_KEY=your_jwt_secret_key_here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=60
JWT_REFRESH_TOKEN_EXPIRE_DAYS=30

# 模型
DEEPSEEK_API_KEY=your_deepseek_api_key
OPENAI_API_KEY=your_openai_api_key

# 旧系统（可选）
LEGACY_MYSQL_URL=mysql://user:password@legacy-db:3306/legacy_db

# 日志
LOG_LEVEL=INFO
```

---

## 5. Dockerfile

### 5.1 API Dockerfile

```dockerfile
# apps/api/Dockerfile
FROM python:3.11-slim

WORKDIR /app

# 安装系统依赖
RUN apt-get update && apt-get install -y \
    gcc \
    postgresql-client \
    && rm -rf /var/lib/apt/lists/*

# 复制依赖文件
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt

# 复制应用代码
COPY . .

# 暴露端口
EXPOSE 8000

# 启动命令
CMD ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--port", "8000"]
```

### 5.2 Web Dockerfile

```dockerfile
# apps/web/Dockerfile
FROM node:18-alpine AS builder

WORKDIR /app

# 复制依赖文件
COPY package.json package-lock.json ./
RUN npm ci

# 复制代码并构建
COPY . .
RUN npm run build

# 生产镜像
FROM nginx:alpine
COPY --from=builder /app/build /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
```

---

## 6. 数据库初始化

### 6.1 init.sql

```sql
-- infra/postgres/init.sql

-- 创建扩展
CREATE EXTENSION IF NOT EXISTS "uuid-ossp";
CREATE EXTENSION IF NOT EXISTS "pgvector";

-- 创建角色
CREATE USER student_agent_app WITH PASSWORD 'app_password';

-- 授权
GRANT ALL PRIVILEGES ON DATABASE student_agent TO student_agent_app;
```

### 6.2 Migration

```bash
# 运行 Migration
docker-compose exec api alembic upgrade head
```

---

## 7. 启动与管理

### 7.1 启动服务

```bash
# 启动所有服务
docker-compose up -d

# 查看日志
docker-compose logs -f

# 查看特定服务日志
docker-compose logs -f api

# 查看服务状态
docker-compose ps
```

### 7.2 停止服务

```bash
# 停止所有服务
docker-compose down

# 停止并删除数据卷
docker-compose down -v
```

### 7.3 重启服务

```bash
# 重启所有服务
docker-compose restart

# 重启特定服务
docker-compose restart api
```

---

## 8. 健康检查

### 8.1 Health Check API

```python
# app/api/v1/health.py
@router.get("/health")
async def health_check():
    """健康检查"""
    return {
        "status": "ok",
        "timestamp": datetime.utcnow().isoformat()
    }

@router.get("/health/db")
async def health_check_db(db: AsyncSession = Depends(get_db)):
    """数据库健康检查"""
    try:
        await db.execute(text("SELECT 1"))
        return {"status": "ok"}
    except Exception as e:
        return {"status": "error", "message": str(e)}

@router.get("/health/redis")
async def health_check_redis(redis: Redis = Depends(get_redis)):
    """Redis 健康检查"""
    try:
        await redis.ping()
        return {"status": "ok"}
    except Exception as e:
        return {"status": "error", "message": str(e)}
```

### 8.2 监控脚本

```bash
#!/bin/bash
# infra/scripts/health-check.sh

# 检查各服务健康状态
check_service() {
    service=$1
    url=$2
    
    response=$(curl -s -o /dev/null -w "%{http_code}" $url)
    
    if [ $response -eq 200 ]; then
        echo "✓ $service is healthy"
        return 0
    else
        echo "✗ $service is unhealthy (HTTP $response)"
        return 1
    fi
}

check_service "API" "http://localhost/api/health"
check_service "Database" "http://localhost/api/health/db"
check_service "Redis" "http://localhost/api/health/redis"
```

---

## 9. 备份与恢复

### 9.1 数据库备份

```bash
#!/bin/bash
# infra/scripts/backup-db.sh

BACKUP_DIR="/backups"
TIMESTAMP=$(date +%Y%m%d_%H%M%S)

# 备份数据库
docker-compose exec -T postgres pg_dump \
    -U postgres \
    -d student_agent \
    > "$BACKUP_DIR/student_agent_$TIMESTAMP.sql"

echo "Backup completed: student_agent_$TIMESTAMP.sql"

# 保留最近 7 天的备份
find $BACKUP_DIR -name "student_agent_*.sql" -mtime +7 -delete
```

### 9.2 数据库恢复

```bash
#!/bin/bash
# infra/scripts/restore-db.sh

BACKUP_FILE=$1

if [ -z "$BACKUP_FILE" ]; then
    echo "Usage: ./restore-db.sh <backup_file>"
    exit 1
fi

# 恢复数据库
docker-compose exec -T postgres psql \
    -U postgres \
    -d student_agent \
    < "$BACKUP_FILE"

echo "Restore completed from: $BACKUP_FILE"
```

---

## 10. 日志管理

### 10.1 日志配置

```python
# app/core/logging.py
import logging
from logging.handlers import RotatingFileHandler

def setup_logging(log_level: str = "INFO"):
    """配置日志"""
    
    # 创建 logger
    logger = logging.getLogger("student_agent")
    logger.setLevel(log_level)
    
    # 文件 Handler（轮转）
    file_handler = RotatingFileHandler(
        "/app/logs/app.log",
        maxBytes=10 * 1024 * 1024,  # 10MB
        backupCount=5
    )
    file_handler.setFormatter(
        logging.Formatter(
            '%(asctime)s - %(name)s - %(levelname)s - %(message)s'
        )
    )
    
    # Console Handler
    console_handler = logging.StreamHandler()
    console_handler.setFormatter(
        logging.Formatter('%(levelname)s - %(message)s')
    )
    
    logger.addHandler(file_handler)
    logger.addHandler(console_handler)
```

### 10.2 日志收集

```yaml
# docker-compose.yml 日志驱动
services:
  api:
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
```

---

## 11. 关键要点

1. **所有服务通过 Docker Compose 编排**
2. **Nginx 作为网关，处理 TLS 和路由**
3. **PostgreSQL + pgvector 作为主数据库**
4. **Redis 用于缓存和队列**
5. **环境变量管理配置**
6. **定期备份数据库**
7. **健康检查确保服务可用**
