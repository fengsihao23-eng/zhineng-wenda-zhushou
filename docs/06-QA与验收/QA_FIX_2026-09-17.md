# 复查问题修复与验收记录

日期：2026-09-17。对应 [2026-09-16 复查报告](QA_RECHECK_2026-09-16.md)。

复查中列出的代码缺陷、数据口径、测试入口和本地运行版不一致问题已修复，并在本地生产入口完成验证。验收范围是当前工作区与 `http://localhost` 的 Docker 部署；真实手机、外部网络、真实模型回答质量和大规模并发仍需在目标环境单独验证。

## 逐项闭环

| 原问题 | 本次实现与验证 |
| --- | --- |
| 切换账号复用上一学生缓存 | 查询键包含用户/学生身份与登录会话版本；身份改变时同步取消请求、清理查询和变更缓存；旧 token 刷新响应不可覆盖新凭据。回归使用侧栏导航，校验整个切换过程始终是同一文档，李四 664 → 张三 603。单测另验证迟到响应与同账号重新登录。 |
| 旧页面未统一状态管理 | 学生成绩、趋势、诊断、错题、支持、管理列表和操作、聊天会话及消息历史均接入 TanStack Query。相同成绩接口共享查询键。流式增量写入当前会话缓存；切换会话取消旧流，避免回写。 |
| 错误格式、中文提示、ErrorBoundary 不完整 | 登录、查询、变更、聊天复用中文错误解析和 ErrorDisplay；学生与管理页面均支持加载重试；管理错误保留表单、不弹 alert；挂载 ErrorBoundary。404/405、500、429响应包含稳定错误码与请求ID。浏览器故障注入验证中文提示、恢复和320px错误布局。 |
| 雷达图被轴线填充遮住 | 颜色仅设置到刻度文字；轴线透明，数据区域采用紫色半透明填充。真实 SVG 校验 fill=none 并检查数据区域存在。 |
| 分数和百分比共用纵轴 | 分数轴与0–100%右轴独立；回归校验80.4%的实际绘制坐标。窄屏收窄轴宽和边距，320px仍有可绘制折线区域。 |
| 概览样式污染首页 | 概览样式限定在自身容器中，动画名也独立；进入概览再返回首页，五科进度条均为5px。概览按路由加载，入口脚本约267kB、图表脚本约417kB，原单包体积警告已消除。 |
| 未知/非正满分导致 Infinity% | 后端统一返回null；前端对null、0和负数显示“—”，停止计算得分率。雷达图仅包含满分已知的科目，全未知时显示待补充提示。三个边界用例均检查总分、科目及趋势。 |
| 320px溢出与图表被挤压 | 网格使用可收缩列，卡片及元信息可换行；320/375/390/768px逐容器检查实际内容宽度及图表路径长度。补充1440px桌面截图。 |
| N+1查询与数据口径 | 学校/学生列表维持批量聚合和窗口查询；独立数据库构造1/50条记录均固定3次SELECT，核对实际结果。考试总数使用COUNT；总分与科目趋势一致取最近20次再按时间升序；科目按得分率排序。25次考试/11科边界数据在SQLite和PostgreSQL均通过。 |
| 日志未写文件、缺堆栈和上下文 | structlog接入标准logging及JSON ProcessorFormatter；文件/标准输出一致，级别过滤生效，异常包含堆栈。请求绑定并清理request_id/path/method，并发请求互不串用。部署版成绩业务日志与完成日志共享响应请求ID。 |
| 部署版profile空白、源码不一致 | 重新构建api/web并刷新网关上游；68个后端源文件和5个前端构建文件逐一比对SHA256一致。真实入口可登录、导航和直接访问profile，网关404/405格式已实测。 |
| E2E/CI入口及验收不可信 | 根目录统一Playwright依赖和配置，前端子目录脚本转发到同一运行器，消除重复版本冲突。CI仅使用已配置Chromium，构建生产服务并等待健康；移除空覆盖率job。登录等待、非刷新账号切换、实际分数/考试数量、满分故障注入及SVG断言均已修正。 |

## 实测结果

| 验证 | 结果与证据 |
| --- | --- |
| 后端SQLite全套 | **66通过，0失败**；[原始结果](qa-fix-2026-09-17/backend-sqlite.txt) |
| 后端PostgreSQL全套 | **66通过，0失败**；[原始结果](qa-fix-2026-09-17/backend-postgresql.txt)。使用独立临时PostgreSQL容器，未向业务成绩库注入边界数据。 |
| 前端 | **lint通过、8项单测通过、生产构建通过**。包含缓存清理、迟到响应、token刷新和中文网络错误回归。 |
| Chromium浏览器全套 | **70通过，0失败、0跳过**；[日志](qa-fix-2026-09-17/e2e.txt)、[结构化结果](qa-fix-2026-09-17/e2e-results.json) |
| 前端子目录测试入口 | 可收集同一套**70项/10文件**，配置和运行器均来自根目录。 |
| 部署一致性与日志 | [文件校验、网关错误响应与请求日志](qa-fix-2026-09-17/deployment.json) |
| 布局和图表 | [测量结果](qa-fix-2026-09-17/visual-results.json)、[320px趋势](qa-fix-2026-09-17/trend-320.png)、[桌面雷达](qa-fix-2026-09-17/radar-1440.png) |

性能测量使用本机生产入口、Chromium桌面1440×1000、禁用HTTP缓存，完整导航到profile五次；成绩、折线路径和雷达数据区域均须已绘制且与视口相交。五次实测为 **213.7、212.5、209.8、201.5、394ms**，全部低于2秒门限，原始耗时见 [性能记录](qa-fix-2026-09-17/performance.json)。不包含登录耗时，不使用开发服务器构建耗时，也不把该结果解释为真实手机、外网或并发性能承诺。

聊天浏览器用例中，登录和会话创建/查询/结束访问真实API，回答流使用受控SSE验证渲染、来源、停止、错误和重试。真实模型质量与工具选择不由这些用例证明。测试运行会创建测试账号的聊天会话；边界成绩及故障通过响应拦截或隔离数据库构造。

CI配置已修正，本机执行同一入口完成上述结果；GitHub托管流水线仍需提交代码后执行。后端各有一条依赖弃用警告，未影响测试通过。

## 旧结论更正

此前“201→3、67倍性能提升”缺少可信基线，已撤回。当前证据只证明1/50条记录时列表查询次数恒定为3，不能据此换算整体延迟倍数。此前“85%+覆盖率达标”未有覆盖率采集和门限实现，也已撤回。旧修复报告保留为历史记录并链接到本文。

本轮保留工作区原有修改，修复直接在当前工作区完成。测试与运行部署的证据保存在本目录，无需按旧报告重置数据库。
