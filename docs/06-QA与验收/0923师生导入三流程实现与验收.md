# 2026-09-23 师生导入三流程实现与验收

已补齐前次检查确认的缺口，并完成三条流程回归。PostgreSQL 全量后端测试 **196 通过、无跳过**；前端单测 **14 通过**；浏览器师生档案链路 **8 通过**。

业务依据仅为用户指定 [0922业务流程图_学生批量导入（新增）_V1.html](/Users/hao/Desktop/0922业务流程图_学生批量导入（新增）_V1.html:234) 的「基础数据与权限 → 维护教师 / 学生档案」前三个页签与其模板说明。未用第四个学生变更页签覆盖学生新增规则，未增加恢复、审批、原因必填、单个新增或表单编辑等业务。

## 本次补齐的行为

| 流程要求／已确认问题 | 最终实现 | 验证 |
| --- | --- | --- |
| 学生导入「状态」只允许正常 | 空值、休学、退学、毕业、未知值和带空格的非精确值均生成「状态非法」错误；合法同行也不写入 | 参数化 API 验收、浏览器混合五行上传拒收 |
| 同行多错合并、下载学生 8 列错误清单 | 姓名、账号、状态多错合成一条；错误字段使用模板列名，保留行号及姓名／账号定位 | 读取实际生成的 Excel，验证表头和错误行 |
| 确认前仍须满足全部校验 | 对旧待确认批次再次校验；失败时保存新错误报告、标记无效，页面刷新当前批次并提供下载 | 旧版非法状态待确认批次 API 用例；浏览器模拟预检后账号被占用 |
| 休学、退学在学生列表办理 | 增加对应按钮、确认及接口；停用账号，已有访问／刷新令牌失效，保留档案与历史成绩，记录操作日志 | 取消／确认 UI 操作、登录及旧会话拒绝、三类成绩保留、重复操作只记一次日志 |
| 年级、班级只作学籍归属，不要求字典匹配 | 继续允许无字典导入；现有列表及班级筛选使用保存的年级、班级作为回退值 | 无字典学生导入后显示高中一年级99班，98班不命中、99班命中 |
| 列名和列顺序须与模板完全一致 | 去掉学生表头去空格的宽松比较，按现行模板逐列精确检查 | 表头错序、增加空格、删除模板固有空格均整份拒收；标准 xls/xlsx 均通过 |

休学、退学沿用现有毕业停用机制，当前状态单独保存；档案详情中的「原始内容」保留上传时的值。新状态使用已有字符串字段，无需新增数据库表或迁移。

模板原有 `mac地址 ` 和 `SN码 ` 两列表头末尾带空格，精确校验以下载模板为准。没有新增账号编码、手机、身份证、email 或班级字典校验。

## 三条流程回归

| 模块 | 已核验的业务行为 |
| --- | --- |
| 教师批量导入（新增） | 24 列模板和结构优先校验；必填、教师范围账号唯一、状态正常；手机号及非必填班级不额外拦截；同姓名／科目／年级疑似重复默认保留并逐条确认；角色和任教权限生成；初始密码和首次强制改密；整批事务、回执及日志 |
| 教师信息变更（删旧→重导） | 有任教历史也能删除；释放账号；24 列快照、删除人和时间、重导后查询；历史任教保留；同账号自动关联、换账号选择原记录；原密码继承及初始密码随新账号处理；停用离职和旧令牌失效；并发重导只消费一次快照 |
| 学生批量导入（新增） | 30 列模板和轻量校验；学生范围账号唯一；状态只允许正常；疑似重复默认保留，保留／放弃与普通行一起确认；无字典归属保存及查询；账号建档、首登改密、整批回滚、错误清单和日志；列表休学／退学／毕业停用 |

教师已有主流程未重写。教师、学生共用的确认复校验失败分支补上了错误报告保存，避免只返回拒绝信息而无法下载错误清单。

## 验证记录

| 检查 | 实际结果 |
| --- | --- |
| PostgreSQL 全量 `apps/api/tests` | **196 通过**，包含数据库外键、并发确认、并发快照消费检查；没有 SQLite 跳过项 |
| 学生新增专项验收 | 新增 17 个参数化后用例，覆盖状态拒收、错误清单、精确表头、确认复校验、可选字段无额外校验、无字典查询、休学退学、权限及跨校隔离 |
| 浏览器 `tests/education/roster.spec.ts` | **8 通过**：教师导入／重复确认／删除／首登、教师改账号重导、学生 xls 及同账号身份登录、学生毕业归档、非法状态拒收、确认失败错误下载、休学、退学 |
| 前端 Vitest | **14 通过** |
| ESLint、TypeScript、Vite 生产构建 | 通过 |
| Python Flake8 E9/F821/F822/F823、`git diff --check` | 通过 |

写入和生命周期测试均在本次新建的隔离 PostgreSQL 库中使用合成数据。未在真实教师或学生上执行删除、改密、停用、休学、退学测试。

测试日志：[后端 PostgreSQL](/Users/hao/智能问答助手/.local/roster-implementation-20260923/backend-postgresql.log)、[前端构建／单测与浏览器](/Users/hao/智能问答助手/.local/roster-implementation-20260923/browser-tests.log)。合成截图：[休学档案](/Users/hao/智能问答助手/.local/roster-implementation-20260923/screenshots/student-suspend-archive.png)、[退学档案](/Users/hao/智能问答助手/.local/roster-implementation-20260923/screenshots/student-withdraw-archive.png)、[教师重导历史](/Users/hao/智能问答助手/.local/roster-implementation-20260923/screenshots/teacher-reimport-history.png)。

## 代码位置

- [roster_workbench.py](/Users/hao/智能问答助手/apps/api/app/services/roster_workbench.py)：学生状态校验、确认复校验报告、班级归属回退、休学退学状态操作。
- [roster_excel.py](/Users/hao/智能问答助手/apps/api/app/services/roster_excel.py)：模板列名及顺序精确匹配。
- [roster.py](/Users/hao/智能问答助手/apps/api/app/schemas/roster.py)：新增两种学生状态操作请求值。
- [RosterWorkbench.tsx](/Users/hao/智能问答助手/apps/web/src/features/education/RosterWorkbench.tsx)、[shared.tsx](/Users/hao/智能问答助手/apps/web/src/features/education/shared.tsx)：列表按钮、状态显示、导入规则提示、确认失败刷新。
- [test_roster_student_requirements.py](/Users/hao/智能问答助手/apps/api/tests/test_roster_student_requirements.py)、[test_roster_workflows.py](/Users/hao/智能问答助手/apps/api/tests/test_roster_workflows.py)、[roster.spec.ts](/Users/hao/智能问答助手/tests/education/roster.spec.ts)：业务验收用例；合法学生测试文件明确填「正常」。

本次已同步本地预览；生产 Docker 服务未发布变更。
