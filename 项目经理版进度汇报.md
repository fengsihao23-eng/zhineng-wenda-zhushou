# 智能问答助手项目进度汇报

**汇报日期**：2026年9月11日  
**当前状态**：黄灯，核心功能已实现，正式验收尚未闭环

## 一句话结论

项目底座、成绩数据链路、诊断权限和学生端页面已经形成可演示版本。当前环境的 API 和前端服务均已启动，BASIC / DIAGNOSIS 两类账号的成绩与诊断数据隔离可以通过接口验证；但真实模型尚未配置，Playwright 端到端测试尚未完成，后端覆盖率约 59%，因此暂不建议对外宣称“整体完成 88%”或“里程碑三已完成”。

## 已完成并已验证

1. **系统底座**
   - React + FastAPI 前后端、Nginx、PostgreSQL 15 + pgvector、Redis 7 已部署。
   - 当前五个容器均为 healthy，数据库迁移已到 `009`。

2. **核心业务能力**
   - 登录、学生成绩、历史趋势、诊断报告、权益分层、会话和平台工作流 API 已实现。
   - 学生端已具备学习总览、成绩趋势、诊断报告、错题分析和智能问答入口。

3. **权限与数据隔离**
   - BASIC 账号可读取成绩和趋势，但返回 0 份诊断报告。
   - DIAGNOSIS 账号可读取成绩和趋势，并返回 1 份诊断报告。
   - 两类账号的 API 登录和数据范围已完成实测。

4. **工程质量门禁**
   - 后端测试：34 个用例通过。
   - 前端 Vitest：1 个用例通过。
   - 前端 lint 和生产构建通过。

## 尚未完成或存在风险

1. **真实模型未接通，影响智能问答验收**
   - 当前健康检查显示没有可用模型 Provider，`real_model_ready=false`。
   - 当前只能证明接口和数据链路存在，不能证明真实大模型问答、Tool Calling 和 SSE 已在当前环境闭环。

2. **缺少浏览器级 E2E 验收**
   - 尚未配置 Playwright，也没有登录 → 成绩趋势 → 诊断权限 → 智能问答的自动化流程测试。

3. **测试覆盖率低于计划目标**
   - 当前后端总覆盖率约 59%，低于计划中的 80% 目标。
   - 现有测试以 API 契约和单元测试为主，真实模型、跨学生隔离、幻觉与越权场景仍需补强。

4. **交付基线不够可追溯**
   - 当前仓库尚无 Git commit，部分最新页面、测试和文档仍处于未提交状态。
   - 现有进度报告中的迁移数、测试数、表数量和前端缺失项已与当前代码不一致。

## 建议的验收口径

当前应表述为：

> 核心底座和主要业务代码已完成，阶段三功能已具备 API 级演示条件；真实模型接入、浏览器端到端测试和质量覆盖率仍未达标，项目处于“功能实现中、验收收口阶段”，不是最终完成状态。

## 下一步行动

| 优先级 | 行动 | 完成标准 |
|---|---|---|
| P0 | 配置真实模型并验证问答链路 | 健康检查 `real_model_ready=true`；完成一次真实模型 Tool Calling 和 SSE 流式问答 |
| P0 | 补齐 Playwright E2E | 覆盖登录、成绩总览、趋势、BASIC/DIAGNOSIS 权限和问答主流程 |
| P1 | 补充质量测试 | 覆盖跨学生隔离、诊断权限边界、幻觉/越权、错误恢复；持续提升覆盖率 |
| P1 | 固化交付基线 | 提交代码、锁定版本、附测试日志和验收记录，重新生成进度报告 |

## 会议上可直接说的 30 秒版本

“目前项目的底座、成绩数据、诊断权限和学生端页面已经能跑起来，两个权限账号的 API 数据隔离也验证通过。现在主要差两类收口工作：一是真实模型还没有接通，二是没有 Playwright 的完整用户流程测试，后端覆盖率也只有约 59%。所以我们可以说核心开发已经进入收尾，但还不能把项目报成 88% 完成或通过最终验收。下一步先完成真实模型和 E2E，再锁定版本做里程碑三验收。”
